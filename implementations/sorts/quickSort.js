const assert = require('assert');

/**
 * @param {*} A 
 * @param {*} start 
 * @param {*} end 
 * 
 * An implementation of quickSort. Originally works of a manually selected pivot (end index), but will
 *  be upgraded to choosing a random pivot so as to achieve O(n log n) amortized runtime.
 */
const quickSort = (A, start, end) => {

}

/**
 * Partitions the inputted array between indices start and end s.t. all elements before
 *  the pivot are less than or equal to the pivot and all elements after are greater than
 *  the pivot.
 */
const partition = (A, start, end) => {

}

const quickSort_driver = () => {
    // assert.deepEqual(mergeSort([1]), [1]);
    // assert.deepEqual(mergeSort([3,0,1]), [0,1,3]);
    // assert.deepEqual(mergeSort([2,4,1,6,8,5,3,7]), [1,2,3,4,5,6,7,8]);
    // assert.deepEqual(mergeSort([-2,-1,6,-4,-7]), [-7,-4,-2,-1,6]);
    // console.log("Test complete.");
}